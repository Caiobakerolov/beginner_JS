// os dados primitivos sao STRING, NUMBER, UNDEFINED, NULL, BOOLEAN

const name = 'Caio'; //string
const name1 = "Caio"; // string
const name2 = `Caio`; //string
const number1 = 10; // number
const number2 = 10.52; // number
let nameStudent; //undefined = nao esta apontando para local nenhum na memoria, porque ele nao foi declarado ainda!
const surnameStudent = null; // null = nao esta apontando para local nenhum na memoria, porque ele nao foi declarado ainda!
const boolean = true; // Boolean = true, false (LOGICA)

